type Stack {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  dependencies: [Dependency!]!
    @relation(name: "Dependencies", onDelete: CASCADE)
}

type Dependency {
  name: String!
  version: String!
  dev: Boolean!
}
